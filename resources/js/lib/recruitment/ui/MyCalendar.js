import React from "react";
import PropTypes from 'prop-types';

export default class MyCalendar extends React.PureComponent {
    constructor(props) {
        super(props);
    }

    render () {
        return (
            <h2>My Calendar view</h2>
        );
    }
}
