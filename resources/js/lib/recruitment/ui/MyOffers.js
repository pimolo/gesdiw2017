import React from "react";
import PropTypes from 'prop-types';

export default class MyOffers extends React.PureComponent {
    constructor(props) {
        super(props);
    }

    render () {
        return (
            <h2>My Offers view</h2>
        );
    }
}
